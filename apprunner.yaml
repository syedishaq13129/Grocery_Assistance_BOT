version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip install -r requirements.txt
run:
  runtime-version: 3.7
  command: python app.py
  network:
    port: 8000
  env:
    - name: PORT
      value: "8000"
